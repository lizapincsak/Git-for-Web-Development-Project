link: https://codepen.io/Pincsak/pen/JjXqRoo

 1. What is Semantic HTML? 

    Semantic HTML gives meaning to conten.

    2. What is HTML used for?

    HTML is used for structure of the building

    3. What is an attribute and where do we put it? 

    An attribute goes in the opening tah. Give more information about tag to the browser. 

    4. What is the h1 tag used for? How many times should I use it on a page?

    h1 tag is used for main title. It is only used once per page. 

    5. Name two tags that have required attributes

        1. <img> src="" is required, while alt="" is optional
        2. <a> href

    6. What do we put in the head of our HTML document? 

    The header typlically has h1, nav, and possibly a div with photos and paragraph. 

    7. What is an id? 

    An id is used when we want to anchor to a section in html. It can only be called once. It's case sensitive with no spaces or special characters. It can have dashes and underscores. You use a # with id. 

    8. What elements can I add an id to? 

    Usually, you add an id to a section, but it can only be called once.  

    9. How many times can I use the same id on a page? 

    You can use the same id ONCE. 

    10. What is a class? 

    A class is used for styling. Only use class when inheritance is not enough. You use a . with class. 

    11. What elements can I add a class to? 

    You can add class to any element. 

    12. How many times can I use the same class on a page? 

    This is the coolest part of a class. You can name multiple items in html the same class. 

    13. How do I get my link to open in a new tab?

    You use target="_blank" after the href="" to have the link open in a new page.  

    14. What is the alt attribute in the image tag used for? 

    alt="" is used to describe the image for screen readers, which are used for people who cannot see. 

    15. How do I reference an id?

    You reference an id, using #.

    16. What is the difference between a section and a div?

        1. <section> is semantic html
        2. <div> does not give meaning to content. It is a container used to move the content around. It's great for flexbox. 

    17. What is CSS used for? 

    CSS is used to make the house beatiful and have style. It's like the interior decorator.

    18. How do we select an element? Example - every h2 on the page

    You can use the universal selector *{} to call everything on the page. Otherwise, if you want to select two different elements at the same time like h2, h3{} you can use a comma. 

    19. What is the difference between a class and an id? - Give me an example of when I might use each one

    The same class can be given to multiple different elements on a page that you want to have the same things done to it. I would use it for flexbox. I typically use class with div. 

    You use id when you want to give an element a very specific name that nothing else will have. 

    20. How do we select classes in CSS?

    We select classes using a .

    21. How do we select a p element with a single class of “human””?

    .class p{}

    22. What is a parent child selector? When would this be useful? 

    A parent child selector has to do with inheritance. A child inherits everything from a parent, but a parent doesn't inherit anything from the child. 

    23. How do you select all links within a div with the class of sidebar?

    .sidebar div{}

    24. What is a pseudo selector?

    A pseudo-class is used to define a special state of an element.

    For example, it can be used to:
        -Style an element when a user hovers over it

    25. What do we use the change the spacing between lines?
    
    line-height


    26. What do we use to change the spacing between letters?

    letter-spacing

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?

    text-transform: capitalize;
    text-transform: uppercase;
    text-transform: lowercase;

    28. How do I add a 1px border around my div that is dotted and black?

    I put a border around the body:
    border-style: dotted;
    border-color: black;
    border-width: 1px;


    29. How do I select everything on the page? 

    universal selector *{}

    30. How do I write a comment in CSS?

    In CSS you write comments using /**/

    31. How do I find out what file I am in, when I am using the command line? 

    cd

    32. Using the command line - how do I see a list of files/folders in my current folder? 

    ls

    33. How do I remove a file via the command line? Why do I have to be careful with this? 

    rm you need to be careful with this because it deletes everything very quickly and you cannot retrieve it

    34. Why should I use version control? 


    35. How often should I commit to github?

    You should commit every 15-20 minutes. 

    36. What is the command we would use to push our repo up to github? 

    git add .
    git commit -m "message"
    git push -u origin "liza-pincsak" or just git push if done it once

    37. Walk me through Lambda's git flow. 
        1. Fork
	    2. Invite mentor
	    3. Click code: https (If forked, should say name)
		4. Copy clipboard
	    5. Terminal
		    a. Cd Desktop/
		    b. Git clone https...
            c. cd into Desktop/name of project
	    6. Git checkout -b 'liza-pincsak'
	    7. Then open in vs code: code .
	    8. Make a change
        9. Git add .
        10. Git commit -m "message to yourself" (make sure it is "")
        11. Git push - u origin 'liza-pincsak' (need to create branch after just use git command)
        12. Change is in branch: click on master and go to name
	    13. Make sure my forked copy: lizapincsak
	    16. Click master
	    17. Click liza
	    18. Click create pull request

Stretch Questions

    1. What is the difference between an inline element and a block element?

    Inline elements: does not take up it's own line, so the elements will be on the same line i.e. span.
    Block elements: has it's own line, so it will be the only thing on that line i.e. div

    2. What happens when an element is positioned absolutely? 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
